<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.independant.cashpoint">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.camera" />
    <uses-permission android:name="android.permission.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.NFC" />

    <uses-feature
        android:name="android.hardware.nfc"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2">
        <activity android:name=".CheckConnexion"></activity>
        <activity android:name=".ScanCarte" />
        <activity android:name=".MyCarte" />
        <activity
            android:name=".AlertActivity"
            android:theme="@style/AppTheme2" />
        <activity android:name=".ManagerActivity" />
        <activity
            android:name=".CentralActivity"
            android:label="@string/title_activity_central"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".login.ConfirmationCompte"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".login.InscriptionActivity"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".login.login"
            android:theme="@style/AppTheme2" />
        <activity android:name=".RechargeActivity" />
        <activity
            android:name=".HomeActivity"
            android:theme="@style/AppTheme2" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>